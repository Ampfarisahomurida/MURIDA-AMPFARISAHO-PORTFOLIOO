services:
  - type: web
    name: murida-portfolio
    env: docker
    plan: free
    branch: main
    dockerfilePath: Dockerfile
    autoDeploy: true
    # No extra env vars required for static site
